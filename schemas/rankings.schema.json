{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://happykhan.github.io/seebot/schemas/rankings.schema.json",
  "title": "Seebot award ranking view",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "rubric_version", "title", "scope_note", "rankings"],
  "properties": {
    "schema_version": {"const": 1},
    "rubric_version": {"type": "string", "minLength": 1},
    "title": {"type": "string", "minLength": 1},
    "scope_note": {"type": "string", "minLength": 1},
    "rankings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["package_id", "name", "version", "build", "subdir", "category", "description", "upstream_url", "run_id", "score", "maximum_points", "eligible", "missing_checks", "tier", "tier_colour", "breakdown", "rank"],
        "properties": {
          "package_id": {"type": "string", "minLength": 1},
          "name": {"type": "string", "minLength": 1},
          "version": {"type": "string", "minLength": 1},
          "build": {"type": "string", "minLength": 1},
          "subdir": {"type": "string", "minLength": 1},
          "category": {"type": "string", "minLength": 1},
          "description": {"type": "string", "minLength": 1},
          "upstream_url": {"type": "string", "format": "uri"},
          "run_id": {"type": "string", "minLength": 1},
          "score": {"type": "number", "minimum": 0, "maximum": 100},
          "maximum_points": {"const": 100},
          "eligible": {"type": "boolean"},
          "missing_checks": {"type": "array", "items": {"type": "string"}, "uniqueItems": true},
          "tier": {"enum": ["Gold", "Silver", "Bronze", "Reviewed"]},
          "tier_colour": {"type": "string", "pattern": "^#[0-9a-fA-F]{6}$"},
          "breakdown": {
            "type": "object",
            "additionalProperties": false,
            "required": ["contracts", "repository", "recipe_test"],
            "properties": {
              "contracts": {"type": "number", "minimum": 0, "maximum": 50},
              "repository": {"type": "number", "minimum": 0, "maximum": 30},
              "recipe_test": {"type": "number", "minimum": 0, "maximum": 20}
            }
          },
          "rank": {"type": ["integer", "null"], "minimum": 1}
        }
      }
    }
  }
}
