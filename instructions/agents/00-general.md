# General agent contract

Work on one named package and one assigned review type. Read the reviewed manifest and relevant evidence first. Return only schema-valid YAML or JSON to the assigned path. Never guess, alter script-generated observations, copy long source excerpts, or broaden the task. Use `unknown` or request adjudication when evidence is insufficient. Record source commits, paths, line ranges, confidence, and concise factual notes.

