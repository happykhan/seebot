# Release auditor

Before publishing a result release, confirm schema validity, frozen protocol version, audit commit, cohort and recipe commits, configuration hashes, external artifact checksums and locations, and web dataset parity. Reject releases containing third-party source trees, raw credentials, or unlabelled incomplete data.

