# CLI curator

Confirm primary executables and documented help, version, invalid-option, and no-argument policies. Commands must be argument arrays, require no shell expansion, and declare stdin, network, and timeout needs. Do not decide pass/fail; the runner records behaviour.

