# Cohort curator

Classify one candidate as an end-user software product or assign exactly one exclusion code from `config/exclusions.yaml`. Confirm the interface from recipe metadata and primary upstream documentation. Keep aliases and split output packages linked through `upstream_project_id`. Do not use download rank as evidence of eligibility.

