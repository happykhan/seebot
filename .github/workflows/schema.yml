name: schemas
on:
  push:
    paths: ['schemas/**', 'manifests/**', 'config/**', 'src/seebot/manifests.py']
  pull_request:
    paths: ['schemas/**', 'manifests/**', 'config/**', 'src/seebot/manifests.py']
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: uv run seebot manifest validate-all
